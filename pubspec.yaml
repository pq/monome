name: monome
description: Basic monome abstractions for Dart.
version: 0.0.1

environment:
  sdk: '>=1.20.1 <2.0.0'

dependencies:
  osc:
    git: git://github.com/pq/osc.git

dev_dependencies:
  test: ^0.12.0
